[Standard — labelling the issue for review] If this is the **in review** milestone (the PR is open), the **dev cost is part of the milestone, not an extra**: run `python3 <plugin>/skills/cost/scripts/session-cost.py` (it already measures **this** issue only, from the flow baseline) and post it on the SAME issue **before labelling** — `gh issue comment <n> --body "..."` with the `<!-- ferion:cost tokens=… usd=… active_h=… wall_h=… -->` marker inline (use `--body`, not `--body-file`: the hook stamps the milestone from what it can see in the command). Already had a value? Read it and **add to it** (`cost`).
No number available (`ccusage` unavailable, cost capture not set up): **label it anyway**, comment what you do have and **say so in the summary** — never invent a cost, never stay silent. The `in progress` milestone has no cost step: what matters there is assuming the issue (assignee) before labelling it (`issues`).
