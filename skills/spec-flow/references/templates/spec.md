# Spec — `<feature name>`

- **Author:** `<fill>` · **Date:** `<fill>` · **Status:** draft | in review | approved
- **Target application/service:** `<fill — see context>`
- **Issue:** `#<n>`

## Problem / context
`<Which pain or opportunity? Why now? Who is affected?>`

## Goal (the why)
`<The desired outcome, in 1–3 sentences. Focus on value, not on the technical solution.>`

## Out of scope
`<What explicitly will NOT be done in this delivery.>`

## User stories
- As a `<persona>`, I want `<action>`, so that `<benefit>`.
- ...

## Acceptance criteria
- [ ] `<verifiable condition 1>`
- [ ] `<verifiable condition 2>`
- [ ] Error/edge cases: `<list them>`

## State matrix (required — Definition of Ready)
> Every entity touched, its domain states and the expected behavior **in each one**. A state with no
> defined behavior is a question for Clarify, not an implementation detail.

| Entity | States | Expected behavior in each |
|---|---|---|
| `<lesson>` | `<published / unpublished>` | `<...>` |
| `<access>` | `<active / expired>` | `<...>` |
| `<content>` | `<public / subscriber-only / private>` | `<...>` |

## Open questions (Clarify)
> Fill in during clarification. Do not move to the plan with a critical item open.
- [ ] `<question>` → **Answer:** `<...>`

## Risks and dependencies
`<Dependencies on other services/teams, known risks.>`

## Compliance notes
`<Impact on sensitive/personal data, security, or constitution principles to observe.>`
